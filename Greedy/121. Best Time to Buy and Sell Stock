class Solution {
    public int maxProfit(int[] prices) {
      int mx = 0 ;
      int st = 0 ;
      for(int i = 1 ; i < prices.length ; i++){
        if(prices[i] > prices[st]){
            mx = Math.max(mx , prices[i]-prices[st]);
        } else st = i ;
      } 
      return mx ; 
    }
}